<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="AnthropicSdkSample.Controls.ApiKeyPromptDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Enter Anthropic API Key"
    PrimaryButtonText="Save"
    CloseButtonText="Exit"
    DefaultButton="Primary">

    <StackPanel Spacing="8" Width="400">
        <PasswordBox x:Name="ApiKeyInput"
                     PlaceholderText="sk-ant-api03-..."
                     HorizontalAlignment="Stretch" />
        <TextBlock
            Text="Your key is stored securely in Windows Credential Manager (DPAPI-encrypted) and never leaves this device."
            TextWrapping="Wrap"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}" />
    </StackPanel>
</ContentDialog>
