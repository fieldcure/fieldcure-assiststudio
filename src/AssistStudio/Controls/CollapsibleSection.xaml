<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AssistStudio.Controls.CollapsibleSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="{x:Bind ContentSpacing}">

        <!-- Header with Toggle Button -->
        <Button
            x:Name="HeaderButton"
            BorderThickness="0"
            Padding="12,4"
            MinHeight="40"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            CornerRadius="4"
            Click="OnHeaderButtonClick"
            AutomationProperties.Name="{x:Bind ToggleAccessibilityName, Mode=OneWay}">

            <Button.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.ThemeDictionaries>
                        <ResourceDictionary x:Key="Default">
                            <StaticResource x:Key="ButtonBackground" ResourceKey="SubtleFillColorTransparentBrush" />
                            <StaticResource x:Key="ButtonBackgroundPointerOver" ResourceKey="SubtleFillColorSecondaryBrush" />
                            <StaticResource x:Key="ButtonBackgroundPressed" ResourceKey="SubtleFillColorTertiaryBrush" />
                        </ResourceDictionary>
                        <ResourceDictionary x:Key="Light">
                            <StaticResource x:Key="ButtonBackground" ResourceKey="SubtleFillColorTransparentBrush" />
                            <StaticResource x:Key="ButtonBackgroundPointerOver" ResourceKey="SubtleFillColorSecondaryBrush" />
                            <StaticResource x:Key="ButtonBackgroundPressed" ResourceKey="SubtleFillColorTertiaryBrush" />
                        </ResourceDictionary>
                        <ResourceDictionary x:Key="Dark">
                            <StaticResource x:Key="ButtonBackground" ResourceKey="SubtleFillColorTransparentBrush" />
                            <StaticResource x:Key="ButtonBackgroundPointerOver" ResourceKey="SubtleFillColorSecondaryBrush" />
                            <StaticResource x:Key="ButtonBackgroundPressed" ResourceKey="SubtleFillColorTertiaryBrush" />
                        </ResourceDictionary>
                    </ResourceDictionary.ThemeDictionaries>
                </ResourceDictionary>
            </Button.Resources>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    Text="{x:Bind Header, Mode=OneWay}"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    VerticalAlignment="Center"/>

                <TextBlock
                    Grid.Column="1"
                    Text="{x:Bind SubHeader, Mode=OneWay}"
                    FontSize="12"
                    Opacity="0.6"
                    Margin="10,0,0,0"
                    VerticalAlignment="Center"/>

                <FontIcon
                    Grid.Column="3"
                    x:Name="ToggleIcon"
                    Glyph="&#xE70E;"
                    FontSize="12"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    RenderTransformOrigin="0.5,0.5"
                    ToolTipService.ToolTip="{x:Bind ToggleTooltip, Mode=OneWay}">
                    <FontIcon.RenderTransform>
                        <RotateTransform x:Name="IconRotation" Angle="{x:Bind InitialIconAngle}"/>
                    </FontIcon.RenderTransform>
                </FontIcon>
            </Grid>
        </Button>

        <!-- Collapsible Content Container -->
        <ContentPresenter
            x:Name="ContentContainer"
            Margin="12,0"
            Content="{x:Bind Body, Mode=OneWay}"
            Visibility="{x:Bind ContentVisibility, Mode=OneWay}"/>
    </StackPanel>
</UserControl>
