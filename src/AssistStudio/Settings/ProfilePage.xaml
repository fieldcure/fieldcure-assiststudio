<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AssistStudio.Settings.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:AssistStudio.Settings"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled">

    <Page.Resources>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Padding="20,14,20,14"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1">
            <TextBlock x:Uid="Profile_Title" Text="Profile"
                       Style="{StaticResource TitleTextBlockStyle}" />
        </Grid>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Padding="20,16">
            <StackPanel Spacing="16" MaxWidth="600" HorizontalAlignment="Left">

                <TextBlock x:Uid="Profile_Description" Text="Configure profiles with system prompts and preferences."
                           Style="{StaticResource CaptionTextBlockStyle}"
                           TextWrapping="Wrap"
                           Opacity="0.7" />

                <!-- Profile list -->
                <StackPanel Spacing="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Uid="Profile_Profiles" Text="Profiles"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   VerticalAlignment="Center" />
                        <Button x:Name="AddProfileButton" Grid.Column="1"
                                x:Uid="Profile_AddProfile"
                                Click="OnAddProfileClicked"
                                ToolTipService.ToolTip="Add profile"
                                Padding="6">
                            <FontIcon Glyph="&#xE710;" FontSize="14" />
                        </Button>
                    </Grid>

                    <ListView x:Name="ProfileListView"
                              SelectionMode="Single"
                              SelectionChanged="OnProfileSelectionChanged"
                              MaxHeight="200">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="4,2" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                    <Button Grid.Column="1" Padding="4"
                                            x:Uid="Profile_DeleteProfile"
                                            Visibility="{Binding IsBuiltIn, Converter={StaticResource InverseBoolToVisibility}}"
                                            Click="OnDeleteProfileClicked"
                                            ToolTipService.ToolTip="Delete"
                                            Style="{StaticResource SubtleButtonStyle}">
                                        <FontIcon Glyph="&#xE74D;" FontSize="12" />
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <!-- Editor -->
                <StackPanel Spacing="8">
                    <TextBox x:Name="ProfileNameBox"
                             x:Uid="Profile_ProfileName"
                             Header="Name"
                             PlaceholderText="Profile name"
                             TextChanged="OnEditorChanged" />
                    <TextBox x:Name="SystemPromptBox"
                             x:Uid="Profile_Placeholder"
                             Header="System Prompt"
                             PlaceholderText="Enter system prompt..."
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="200"
                             TextChanged="OnEditorChanged" />

                    <!-- Preferred Provider -->
                    <ComboBox x:Name="ProviderCombo"
                              x:Uid="Profile_PreferredProvider"
                              Header="Preferred Provider"
                              HorizontalAlignment="Stretch"
                              SelectionChanged="OnProviderChanged" />

                    <!-- Preferred Model -->
                    <ComboBox x:Name="ModelCombo"
                              x:Uid="Profile_PreferredModel"
                              Header="Preferred Model"
                              HorizontalAlignment="Stretch"
                              IsEnabled="False"
                              SelectionChanged="OnModelChanged" />

                    <!-- Tools -->
                    <StackPanel Spacing="4">
                        <TextBlock x:Uid="Profile_Tools" Text="Tools"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <StackPanel x:Name="ToolsPanel" Spacing="4" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
